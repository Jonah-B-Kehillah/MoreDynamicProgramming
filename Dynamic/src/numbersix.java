public class numbersix {
	/*
	Computing edit distance between two strings of length $n$. (The goal is $O(n^2)$ time.)
	One nice feature of this problem is that it has a variation that the students can try afterwards:
	(1) Find the longest common subsequence between two strings.
	 */
}
