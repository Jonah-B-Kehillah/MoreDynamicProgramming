public class challenge {
	/*
	Challenge problem: Given a set of n items with sizes s_1, ..., s_n \in [m] (so each size is an integer from 1 to m), and given a number k, determine if there is a subset of the items whose total size is exactly k. (The goal is $O(nm)$ time.)
	 */
}
